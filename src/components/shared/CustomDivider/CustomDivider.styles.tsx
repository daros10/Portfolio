import { makeStyles } from "@material-ui/core/styles";

export const customDividerStyles = makeStyles({
  divider: {
    backgroundColor: "white",
    height: "15px",
    textAlign: "center",
    marginLeft: "2px",
    marginRight: "2px",
  },
});
